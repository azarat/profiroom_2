import { MonthAdminPipe } from './month-admin.pipe';

describe('MonthAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
