import { AdminMonthPipe } from './admin-month.pipe';

describe('AdminMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
